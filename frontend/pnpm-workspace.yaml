packages:
  - '.'

# These packages ship prebuilt binaries via optionalDependencies; their
# install scripts are not needed, so decline them to keep installs hermetic.
allowBuilds:
  core-js: false
  esbuild: false
  sharp: false
  unrs-resolver: false